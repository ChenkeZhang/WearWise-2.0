// NOTE: In a real production build, these should be environmental variables.
// For this generated code, we assume the user will replace them or they are injected via process.env
// You need to get a free API key from https://openweathermap.org/api
export const OPENWEATHER_API_KEY = process.env.OPENWEATHER_API_KEY || '3806f393ab45ce4b1b18655b0d5733c0';

export const AI_MODEL_NAME = 'gemini-2.5-flash';

export const DEFAULT_LOCATION = {
  lat: 40.7128, // New York
  lon: -74.0060
};